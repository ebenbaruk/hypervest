# ============================================================
# SENSITIVE — never commit
# ============================================================
contracts/.env

# ============================================================
# PROJECT-SPECIFIC — internal tooling, not for judges
# ============================================================
CLAUDE.md
WALLETS.md
.history/

# ============================================================
# FOUNDRY build artifacts
# ============================================================
contracts/out/
contracts/cache/
# contracts/lib/ intentionally NOT ignored — deps are bundled
#   so judges can run `forge build` without `forge install`
# contracts/broadcast/ intentionally NOT ignored — deployment
#   records are useful for judges to verify on-chain activity

# ============================================================
# FRONTEND
# (frontend/.gitignore also covers these within that directory)
# ============================================================
frontend/.env.local
frontend/.env*.local
frontend/node_modules/
frontend/.next/
frontend/.turbo/
frontend/coverage/
frontend/*.tsbuildinfo
frontend/next-env.d.ts
frontend/.vercel

# ============================================================
# SYSTEM
# ============================================================
.DS_Store
Thumbs.db
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
